# Blog

My blog. Generated with Hugo, deployed by GitHub Actions, served by GitHub Pages, and fronted by Cloudflare CDN.

## Producing

On new clones, run `git submodule update --init` to fetch themes.

On new systems, run `brew install hugo` to install the hugo binary.

Locally, serve with `hugo serve`. Publish by committing and pushing; CI takes it from there.