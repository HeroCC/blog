<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Posts on CC's Sink</title><link>/posts/</link><description>Recent content in Posts on CC's Sink</description><generator>Hugo -- gohugo.io</generator><language>en</language><copyright>CC BY-SA 4.0</copyright><lastBuildDate>Tue, 14 Apr 2026 18:00:00 +0200</lastBuildDate><atom:link href="/posts/index.xml" rel="self" type="application/rss+xml"/><item><title>Jumping Ship: Migrating your container images from DockerHub to Quay.io</title><link>/posts/2020-jumping-ship-dockerhub-quay/</link><pubDate>Mon, 14 Sep 2020 18:31:03 +0200</pubDate><guid>/posts/2020-jumping-ship-dockerhub-quay/</guid><description>Here are the steps I took to move my images from DockerHub to Quay. If you&amp;rsquo;re not interested in my thoughts or background, or know what you&amp;rsquo;re doing, scroll down to &amp;ldquo;Moving the Images&amp;rdquo;.
Background I host several repositories at DockerHub, both for work and personally. When DockerHub announced they would be deleting container images not used within 6 months, I understood. They provide a great service to the community, and frankly I&amp;rsquo;m surprised this didn&amp;rsquo;t happen sooner.</description><content type="html"><![CDATA[<p>Here are the steps I took to move my images from DockerHub to Quay. If you&rsquo;re not interested in my thoughts or background, or know what you&rsquo;re doing, scroll down to &ldquo;Moving the Images&rdquo;.</p>
<h2 id="background">Background</h2>
<p>I host several repositories at DockerHub, both for work and personally.
When DockerHub announced they would be <a href="https://www.docker.com/pricing/resource-consumption-updates">deleting container images</a> not used within 6 months, I understood.
They provide a great service to the community, and frankly I&rsquo;m surprised this didn&rsquo;t happen sooner. Docker Inc., and it&rsquo;s (new-ish) owner Mirantis, don&rsquo;t have the cash to burn like the Google and Amazon do.
Still though, it&rsquo;s a bit sad, and can be frusterating. I believe DockerHub suffers from the same issue that <a href="https://stackoverflow.com/q/13321556/1709894">GitHub / Git</a> does. For people not in-the-know, they are synonyms. This is perpetuated by the fact that the default docker registry for everyone using the Docker container implementation, is docker.io.</p>
<p>If you weren&rsquo;t aware, whether you&rsquo;re new or never looked into the matter much, the command <code>docker pull herocc/my-cool-image:latest</code> actually maps to <code>docker pull docker.io/herocc/my-cool-image:latest</code>. See the difference there?
The default container registry for <code>docker</code>, is DockerHub. They look similar, but aren&rsquo;t the same.</p>
<p>Nonetheless, docker (the container engine) and DockerHub (the container registry) aren&rsquo;t the same. And now, lots of images (<em>4.5PB worth!!</em>) are slated for deletion. You have two options: pony up for a premium <a href="https://www.docker.com/pricing">DockerHub subscription</a> for you or your company, or if you <em>really</em> need those oft-unused images to still be publicly avaliable with a $0 budget, you can move to a another registry, such as quay.io.</p>
<p><a href="https://quay.io">Quay.io</a> is another container registry, which as of this writing, offers a free public repo with unlimited retention. It is run by RedHat (and it&rsquo;s parent company, IBM), which has been making huge investments into containerization tech. I trust it will stick around for a while, and has a lot of nice features compared to DockerHub which I wasn&rsquo;t aware of before (though that&rsquo;s a point for another article).</p>
<p>Anyway, on to the part you actually cared about: migrating the images.</p>
<h2 id="getting-ready">Getting Ready</h2>
<p>You&rsquo;re going to need an account on Quay to be able to push new images. Navigate to <a href="https://quay.io/repository/">quay</a> and sign in or make an account. Then, click the &ldquo;Create new repository&rdquo; button. Fill this in as you like. For mine, I selected &ldquo;Public&rdquo;, and &ldquo;(Empty repository)&rdquo;. You can look into using Quay for your builds if you&rsquo;d like, but that&rsquo;s out of scope for this post.</p>
<p>Once you have your repo, you&rsquo;re going to need to sign in to quay locally to get permission to push image tags. Technically you can <code>docker login quay.io</code> with the username and password you just used, but for better security, you should follow these next steps. Click your username in the top right corner, click &ldquo;Account Settings&rdquo;, and &ldquo;Generate Encrypted Password&rdquo;. Once you get through the prompts, you&rsquo;ll find yourself with a long string of text. You can now run <code>docker login quay.io</code>, providing your standard username, and the encrypted password.</p>
<p>You should see something along the lines of &ldquo;Successful Login&rdquo;, and once you do, you&rsquo;re ready for the next step.</p>
<h2 id="moving-the-images">Moving the Images</h2>
<p>One way of moving the images is to pull them each down to your computer, <code>docker tag my-old/repo:tag quay.io/my-new/repo:tag</code>, but that is slow and inefficient. You need to manually pull each tag from the old registry, re-tag it, and push it back up again. If you have a lot of images, this could take a long time. Fortunately, there&rsquo;s an alternative: Skopeo.</p>
<p>Skopeo is a &ldquo;command line utility that performs various operations on container images and image repositories&rdquo;. One of the features it provides is copying images from one registry to another &ndash; either one-off, or syncing entire repos and registries. We will be using their <code>skopeo sync</code> feature to move over all of our tags.</p>
<p>First, lets install skopeo. This depends on your operating system, so you should read <a href="https://github.com/containers/skopeo/blob/master/install.md">their installation instructions</a> and come back once you&rsquo;ve finished.</p>
<p>Once you finished installing skopeo, you should see something like this:</p>
<pre tabindex="0"><code>$ skopeo --version
skopeo version 1.1.1
</code></pre><p>If you do, that means you&rsquo;re ready to go! Make sure you&rsquo;re logged in to quay by running <code>skopeo login quay.io</code>, and you&rsquo;re ready to construct your command.</p>
<p>If your old image repo is hosted on DockerHub, it&rsquo;s full link is <code>docker.io/YOUR-ORG-OR-NAME/REPO-NAME</code>. Your repo on quay, the one you just made, is likely similar, <code>quay.io/YOUR-ORG-OR-NAME/REPO-NAME</code>. All you have to do is tell skopeo to sync one to the other.</p>
<p>If you have a lot of images this may take a while, so consider starting a <code>screen</code> session (something like <code>screen -S quaysync</code>). Then, replace the links below with your repo, and hit enter:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>$ skopeo sync --src docker --dest docker docker.io/YOUR-ORG/REPO-NAME quay.io/YOUR-ORG
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>INFO<span style="color:#f92672">[</span>0000<span style="color:#f92672">]</span> Tag presence check              imagename<span style="color:#f92672">=</span>docker.io/org/repo tagged<span style="color:#f92672">=</span>false
</span></span><span style="display:flex;"><span>INFO<span style="color:#f92672">[</span>0000<span style="color:#f92672">]</span> Getting tags                    image<span style="color:#f92672">=</span>docker.io/org/repo
</span></span><span style="display:flex;"><span>INFO<span style="color:#f92672">[</span>0000<span style="color:#f92672">]</span> Copying image tag 1/2295        from<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;docker://org/repo:r8842&#34;</span> to<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;docker://quay.io/org/repo:r8842&#34;</span>
</span></span><span style="display:flex;"><span>...
</span></span></code></pre></div><p>Skopeo will index all of the tags, and one by one copy the relevant blobs from DockerHub to Quay. You can refresh Quay to see this as it progresses.</p>
<p>And that&rsquo;s it! Once skopeo finishes, your tags will be safe over on quay. You should tell users to pull images from the new source, and make sure your CI system is set to push there instead (or at least schedule a cron job to sun <code>skopeo</code> again).</p>
<p>I hope this helped you in some way and that you enjoyed the read. Until next time, cya!</p>
]]></content></item><item><title>Cloudflare with LetsEncrypt SSL Certificates + Apache's ProxyPass</title><link>/posts/2016-cloudflare-letsencrypt/</link><pubDate>Tue, 08 Nov 2016 14:53:00 +0000</pubDate><guid>/posts/2016-cloudflare-letsencrypt/</guid><description>So, you want to use LetsEncrypt to generate a free SSL certificate for your site behind Cloudflare and Apache (acting as a ReverseProxy). After many hours of research and tweaking, I got this setup to work. Here I will document my process for you to use. In the end, your setup will look something like this: User &amp;lt;-(SSL)-&amp;gt; Cloudflare &amp;lt;-(SSL)-&amp;gt; Apache &amp;lt;&amp;ndash;&amp;gt; Your Webapp (not SSL). This guide assumes you have a site already setup without SSL.</description><content type="html"><![CDATA[<p>So, you want to use <a href="https://letsencrypt.org/">LetsEncrypt</a> to generate a free SSL certificate for your site behind Cloudflare and Apache (acting as a ReverseProxy). After many hours of research and tweaking, I got this setup to work. Here I will document my process for you to use. In the end, your setup will look something like this: <em>User &lt;-(SSL)-&gt; Cloudflare &lt;-(SSL)-&gt; Apache &lt;&ndash;&gt; Your Webapp (not SSL)</em>. This guide assumes you have a site already setup without SSL. I am using Ubuntu 16.04 for my setup, but the process should be similar for other OSes.</p>
<h2 id="getting-started">Getting Started</h2>
<p>Before we do anything, we need to install LetsEncrypt. LetsEncrypt reccomends <a href="https://certbot.eff.org/">Certbot</a> as their client. Unless you know what you are doing, you should use this. Follow the instructions on their site to install the Certbot client. After Certbot is installed, you should install the apache mod <em>mod_cloudflare</em>. It isn&rsquo;t required, but is good in any setup invloving Cloudflare, as it resolves IPs to their actual source, not Cloudflare&rsquo;s IPs. Follow the instructions on <a href="https://www.cloudflare.com/technical-resources/#mod_cloudflare">Cloudflare&rsquo;s site</a>, and run <code>sudo a2enmod cloudflare</code> to ensure it is enabled. Finally, make a backup of the the site configs you will be changing. It is unlikely something will go wrong, but just in case we need to roll back we should have one.</p>
<h2 id="custom-config-patch">Custom Config Patch</h2>
<p>Now that Certbot/LE is installed, we will continue on with the setup. By default, apache passes all traffic to the backend application when using ProxyPass. We need to add an exception to this rule by installing and including a custom config. <em>If you aren&rsquo;t using ProxyPass, skip this step</em>. Make a new file in <code>/etc/apache2/conf-avaliable/letsencrypt-proxypass-fix.conf</code>, and paste the following into it:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-xml" data-lang="xml"><span style="display:flex;"><span># From https://github.com/certbot/certbot/issues/2164
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;IfModule</span> <span style="color:#960050;background-color:#1e0010">mod_proxy.c</span><span style="color:#f92672">&gt;</span> # If Proxy is installed
</span></span><span style="display:flex;"><span>    ProxyPass &#34;/.well-known/acme-challenge&#34; &#34;!&#34; # Do not proxy the acme verification
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;/IfModule&gt;</span>
</span></span><span style="display:flex;"><span>Alias /.well-known/acme-challenge /var/www/html/.well-known/acme-challenge # Send all requests for the acme verification to this location
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;Directory</span> <span style="color:#960050;background-color:#1e0010">&#34;/var/www/html/.well-known/acme-challenge&#34;</span><span style="color:#f92672">&gt;</span> # Allow any requests for it
</span></span><span style="display:flex;"><span>    Options None
</span></span><span style="display:flex;"><span>    AllowOverride None
</span></span><span style="display:flex;"><span>    Require all granted
</span></span><span style="display:flex;"><span>    AddDefaultCharset off
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;/Directory&gt;</span>
</span></span></code></pre></div><p>Save the file, and quit. For now this isn&rsquo;t activated, but we will be including it in the site configuration file next.</p>
<h2 id="generating-the-certificate">Generating the Certificate</h2>
<p>Now that most of the prep work is finished, we can get to actually generating the certificate. Include the line <code>Include /etc/apache2/conf-available/letsencrypt-proxypass-fix.conf</code> in the site&rsquo;s config, and reload apache. Now, run <code>certbot certonly --webroot --webroot-path /var/www/html/ --renew-by-default -d YOUR.DOMAIN.HERE</code>. Answer the questions, and assuming everything was done correctly, you will get a message saying that the certificate was generated successfully, and will expire in 90 days. Whoopee, you have an SSL certificate! However, even though you were issued an SSL certificate, apache doesn&rsquo;t know about it, and is still only listening for non-secure connections. We need to install the certificate for apache to use.</p>
<h2 id="installing-the-certificate">Installing the Certificate</h2>
<p>Now that you have generated the certificate, you need to install it to apache. Open your site&rsquo;s config, and edit it to look something like the following. Note, some applications require extra flags and steps, so please follow their documentation as well; this is a minimal config. Also, don&rsquo;t forget to include any other configurations you had from your old config!</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-xml" data-lang="xml"><span style="display:flex;"><span><span style="color:#f92672">&lt;VirtualHost</span> <span style="color:#960050;background-color:#1e0010">*:80</span><span style="color:#f92672">&gt;</span>
</span></span><span style="display:flex;"><span>ServerName YOUR.DOMAIN.HERE
</span></span><span style="display:flex;"><span>Redirect permanent / https://YOUR.DOMAIN.HERE/ # Redirect all nonsecure requests to use HTTPS
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;/VirtualHost&gt;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;VirtualHost</span> <span style="color:#960050;background-color:#1e0010">*:443</span><span style="color:#f92672">&gt;</span>
</span></span><span style="display:flex;"><span>ServerName YOUR.DOMAIN.HERE
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>SSLProxyEngine On
</span></span><span style="display:flex;"><span>SSLCertificateFile /etc/letsencrypt/live/YOUR.DOMAIN.HERE/cert.pem
</span></span><span style="display:flex;"><span>SSLCertificateKeyFile /etc/letsencrypt/live/YOUR.DOMAIN.HERE/privkey.pem
</span></span><span style="display:flex;"><span>SSLCertificateChainFile /etc/letsencrypt/live/YOUR.DOMAIN.HERE/chain.pem
</span></span><span style="display:flex;"><span>Include /etc/letsencrypt/options-ssl-apache.conf # The LetsEncrypt SSL config
</span></span><span style="display:flex;"><span>Include /etc/apache2/conf-available/letsencrypt-proxypass-fix.conf # The patch for ProxyPass we made earlier
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>ProxyRequests Off
</span></span><span style="display:flex;"><span>ProxyPreserveHost On
</span></span><span style="display:flex;"><span>ProxyPass / http://000.000.000.000/ # Replace this with whatever address you had from your old config
</span></span><span style="display:flex;"><span>ProxyPassReverse / http://000.000.000.000/
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>RequestHeader append &#34;X-Forwarded-Proto&#34; &#34;https&#34; # Tell the application we are forwarding from SSL
</span></span><span style="display:flex;"><span>RequestHeader set &#34;X-Forwarded-Ssl&#34; &#34;on&#34;
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;/VirtualHost&gt;</span>
</span></span></code></pre></div><p>Tada! Your service now uses your certificate. If you were to visit this site without Cloudflare&rsquo;s CDN you would be able to see that the site uses LetsEncrypt.</p>
<h2 id="configuring-cloudflare">Configuring Cloudflare</h2>
<p>Now that HTTPS is installed, we need to tell Cloudflare to use Full SSL. I personally have had mixed experiences, but sometimes if this isn&rsquo;t set properly, your browser can get stuck in a Redirect loop. Full SSL requires that your server has a certificate installed for users to access HTTPS via Cloudflare. Changing this option affects all records on the domain, so don&rsquo;t do this unless everything is set up correctly (alternativley you can use a <a href="https://support.cloudflare.com/hc/en-us/articles/200170536-How-do-I-redirect-all-visitors-to-HTTPS-SSL-">Page Rule</a>). For a more detailed explanation, see <a href="https://support.cloudflare.com/hc/en-us/articles/200170416-What-do-the-SSL-options-mean-">Cloudflare&rsquo;s support article</a>.</p>
<p>Once you are logged into Cloudflare&rsquo;s website, click your domain, click the <em>Crypto</em> button, and select <em>Full (Strict)</em> from the dropdown. Now Cloudflare will only use your valid LetsEncrypt Certificate for communication!</p>
<h2 id="wrap-up">Wrap Up</h2>
<p>If all went according to plan, you should be able to naivgate your site from inside or ourside Cloudflare via HTTPS. Make sure you update your site&rsquo;s links to use HTTPS!
Don&rsquo;t forget every month or so to renew the certificate you just made with <code>certbot renew</code>. I will make another post later on how to automatically renew your certs, eliminating the need for maintenance.</p>
<p>I hope this guide helped you. If you have any questions or comments, don&rsquo;t be afraid to comment or contact me!</p>
]]></content></item></channel></rss>